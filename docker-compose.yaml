# To execute this docker-compose yml file use `docker-compose -f docker-compose.yml up`
# Add the `-d` flag at the end for detached execution
# To stop the execution, hit Ctrl+C, and then `docker-compose -f docker-compose.yml down`
version: "3"
services:
  # main_contact_project:
  #   build:
  #     context: .
  #     dockerfile: ./Dockerfile
  #   container_name: main-contact_project
  #   # restart: always
  #   networks:
  #    - default
  #   volumes:
  #     - /home/abdelghafour.mouhsine/jupyter/contact_project/Contact_Project:/app
  #   depends_on:
  #     - selenium_hub_contact_project
  #     - ollama_contact_project
  #   command: ["python", "./main.py"]

  # jupyter_contact_project:
  #   build:
  #     context: .
  #     dockerfile: ./Dockerfile
  #   container_name: jupyter-contact-project
  #   environment:
  #     - JUPYTER_TOKEN=99
  #   restart: always
  #   ports:
  #    - 10000:8888
  #   networks:
  #    - default
  #   volumes:
  #     - /home/abdelghafour.mouhsine/jupyter/contact_project/Contact_Project:/home/jovyan/work

  ollama_contact_project:
    image: ollama/ollama
    container_name: ollama-contact_project
    restart: always
    volumes:
      - ./contactLLM/ollama:/root/.ollama
    ports:
      - 11000:11434
    networks:
     - default

  chrome_1_contact_project:
    image: selenium/node-chrome:4.18.1-20240224
    shm_size: 2gb
    depends_on:
      - selenium_hub_contact_project
    environment:
      - SE_EVENT_BUS_HOST=selenium_hub_contact_project
      - SE_EVENT_BUS_PUBLISH_PORT=4442
      - SE_EVENT_BUS_SUBSCRIBE_PORT=4443
      - SE_NODE_OVERRIDE_MAX_SESSIONS=true
      - SE_NODE_MAX_SESSIONS=10
  
  chrome_2_contact_project:
    image: selenium/node-chrome:4.18.1-20240224
    shm_size: 2gb
    depends_on:
      - selenium_hub_contact_project
    environment:
      - SE_EVENT_BUS_HOST=selenium_hub_contact_project
      - SE_EVENT_BUS_PUBLISH_PORT=4442
      - SE_EVENT_BUS_SUBSCRIBE_PORT=4443
      - SE_NODE_OVERRIDE_MAX_SESSIONS=true
      - SE_NODE_MAX_SESSIONS=10

  chrome_3_contact_project:
    image: selenium/node-chrome:4.18.1-20240224
    shm_size: 2gb
    depends_on:
      - selenium_hub_contact_project
    environment:
      - SE_EVENT_BUS_HOST=selenium_hub_contact_project
      - SE_EVENT_BUS_PUBLISH_PORT=4442
      - SE_EVENT_BUS_SUBSCRIBE_PORT=4443
      - SE_NODE_OVERRIDE_MAX_SESSIONS=true
      - SE_NODE_MAX_SESSIONS=10

  chrome_4_contact_project:
    image: selenium/node-chrome:4.18.1-20240224
    shm_size: 2gb
    depends_on:
      - selenium_hub_contact_project
    environment:
      - SE_EVENT_BUS_HOST=selenium_hub_contact_project
      - SE_EVENT_BUS_PUBLISH_PORT=4442
      - SE_EVENT_BUS_SUBSCRIBE_PORT=4443
      - SE_NODE_OVERRIDE_MAX_SESSIONS=true
      - SE_NODE_MAX_SESSIONS=10
  

  selenium_hub_contact_project:
    image: selenium/hub:4.18.1-20240224
    container_name: selenium-hub-contact-project
    ports:
      - "4442:4442"
      - "4443:4443"
      - "4444:4444"
      
networks:
   default:
      external:
         name: network-gw-3-1